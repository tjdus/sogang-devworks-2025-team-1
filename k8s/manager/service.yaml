apiVersion: v1
kind: Service
metadata:
  name: manager-svc
  namespace: agent-system
  labels:
    app: manager
spec:
  selector:
    app: manager
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  type: ClusterIP
