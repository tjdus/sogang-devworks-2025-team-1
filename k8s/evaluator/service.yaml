apiVersion: v1
kind: Service
metadata:
  name: evaluator-svc
  namespace: agent-system
  labels:
    app: evaluator
spec:
  selector:
    app: evaluator
  ports:
    - port: 8002
      targetPort: 8002
      name: http
  type: ClusterIP
