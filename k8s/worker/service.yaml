apiVersion: v1
kind: Service
metadata:
  name: worker-svc
  namespace: agent-system
  labels:
    app: worker
spec:
  selector:
    app: worker
  ports:
    - port: 8001
      targetPort: 8001
      name: http
  type: ClusterIP
